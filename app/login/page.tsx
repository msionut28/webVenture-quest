import type { Metadata } from "next"

export const metadata: Metadata = {
  title: 'Login',
  description: 'Log in so you can get started'
}

const Login = () => {
    return (
        <div>Login</div>
    )
}

export default Login